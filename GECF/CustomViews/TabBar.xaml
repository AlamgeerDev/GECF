<?xml version="1.0" encoding="utf-8" ?>
<Grid xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      BackgroundColor="#043687"
             x:Class="GECF.CustomViews.TabBar">
    <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand">
           <!--<Image Margin="15,0,0,0" IsVisible="{OnPlatform Android=false, iOS=true}" Command="{Binding BackCommand}" ImageSource="backm.png" BackgroundColor="Transparent" HeightRequest="30" WidthRequest="30" />-->
        <ImageButton Margin="15,0,0,0"   Command="{Binding BackCommand}"  Source="backm.png" BackgroundColor="Transparent" HeightRequest="30" WidthRequest="30"/>
    </StackLayout>

    <Image Margin="0,25,0,0" Source="logo_white" HeightRequest="20" WidthRequest="20" HorizontalOptions="Center" VerticalOptions="Start"/>
</Grid>

