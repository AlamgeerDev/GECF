<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:control="clr-namespace:GECF.CustomViews"
             x:Class="GECF.Views.AboutPage"
              xmlns:appResources="clr-namespace:GECF.Resources"
               BackgroundImageSource="bg_back"
             Title="AboutPage">
    <StackLayout Spacing="0">
      <BoxView HeightRequest="10"
               IsVisible="{OnPlatform Android=false, iOS=true}"
               BackgroundColor="#043687"/>

        <StackLayout VerticalOptions="Start" MinimumHeightRequest="100" HeightRequest="100">
            <control:TabBar HeightRequest="100"/>
       
    </StackLayout>

       
     <StackLayout>

        <Label TextColor="White"   FontFamily="museo500"  FontSize="20"   Text="{x:Static appResources:AppResources.about_us}" HorizontalOptions="Center" Padding="15,20,15,0" />

       <Image HorizontalOptions="FillAndExpand" Aspect="AspectFill" HeightRequest="150" Source="gecfoverview.jpg"/>
          <ScrollView  Orientation="Vertical">

        <Label TextColor="White" Margin="15,20,15,5"  FontFamily="museo500"  FontSize="16"   Text="{Binding SelectedItem}" HorizontalOptions="Center"  />

         </ScrollView>
        
     </StackLayout>
     
   

      
</StackLayout>
</ContentPage>
